import { render } from '@testing-library/react'
import React from 'react'
import SuperInput from './SuperInput'

describe('SuperInput', () => {
  test('renders', () => {
    render(<SuperInput />)
  })
})
